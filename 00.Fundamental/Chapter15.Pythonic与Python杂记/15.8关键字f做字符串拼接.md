# 关键字 f 做字符串拼接

```python

a = 'python'

b = len(a)

# 没有使用 关键 f 做字符串拼接
print("长度大于5, 且真实长度为 " + str(b))

# 使用 关键字 f 做字符串拼接
print(f"长度大于5, 且 {a} 的真实长度为 {b}" )

# 长度大于5, 且真实长度为 6
# 长度大于5, 且 python 的真实长度为 6
```